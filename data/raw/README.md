# Raw Data Directory

This folder holds the raw temperature anomaly file (NetCDF `.nc` datasets) fetched by `code/01_fetch_data.R`.

**Note:** Raw datasets placed here are large and ignored by version control as listed in `.gitignore`.